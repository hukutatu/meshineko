<%= form_with(url: public_recipe_comments_path(recipe_id: recipe.id), local: false) do |f| %>
  <%= f.text_area :opinion, rows:'5',placeholder: "コメントをここに", class: "w-100 comment-textarea" %>
  <%= f.submit "送信する", class: "btn btn-lg btn-base-1 mt-20 pull-right" %>
<% end %>